PROJECT(Fib)
CMAKE_MINIMUM_REQUIRED(VERSION 2.9)
INCLUDE_DIRECTORIES(
    ${CMAKE_BINARY_DIR}/../include
)
AUX_SOURCE_DIRECTORY(
    ${CMAKE_BINARY_DIR}/../SRC
    DIR_SRC
)
ADD_EXECUTABLE(
    Fib
    ${DIR_SRC}
)